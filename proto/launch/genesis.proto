syntax = "proto3";
package tendermint.spn.launch;

// this line is used by starport scaffolding # genesis/proto/import
import "launch/request.proto";
import "launch/vested_account.proto";
import "launch/genesis_account.proto";
import "launch/chain.proto";

option go_package = "github.com/tendermint/spn/x/launch/types";

// GenesisState defines the launch module's genesis state.
message GenesisState {
	// this line is used by starport scaffolding # genesis/proto/state
	repeated Chain chainList = 1; // this line is used by starport scaffolding # genesis/proto/stateField
	repeated ChainNameCount chainNameCountList = 2; // this line is used by starport scaffolding # genesis/proto/stateField
	repeated GenesisAccount genesisAccountList = 3; // this line is used by starport scaffolding # genesis/proto/stateField
	repeated VestedAccount vestedAccountList = 4; // this line is used by starport scaffolding # genesis/proto/stateField
	repeated Request requestList = 10; // this line is used by starport scaffolding # genesis/proto/stateField
	repeated RequestCount requestCountList = 11; // this line is used by starport scaffolding # genesis/proto/stateField
	// this line is used by starport scaffolding # ibc/genesis/proto
}

message RequestCount {
		string chainID = 1;
		uint64 count = 2;
}